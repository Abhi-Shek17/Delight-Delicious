<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" typr="text/css" href="stylehome.css">
    <link rel="stylesheet" typr="text/css" href="stylemenu.css">
    <title>Restaurant</title>
</head>
<body>
    <%- include('./partials/nav.ejs') %>
    <header class="top">
        <h2 class="resname">DELIGHT DELICIOUS</h2>
        
    </header>
    <div class="invisible">YOU are ON top</div>
        <h1 class="menu">Menu</h1>
        <div class="itemslist">
        <% if(item.length >0) {%>
            <% item.forEach(item=>{ %>
                <div class="item">
                    <img width=200px src="<%= item.image %>" alt="<%= item.name %> " class="pic">
                    <div class="itemdesc">
                        <h3 class="itemname"><%= item.name %></h3>
                        <button class="cart">+ ADD</button>
                        <div class="price"> <%= item.price%> </div>
                    </div>
                </div>
              <% }) %>
            <% } %>
        </div>
    <main>

    </main>
    <%- include('./partials/footer.ejs') %>
</body>
</html>